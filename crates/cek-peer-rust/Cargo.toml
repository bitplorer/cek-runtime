[package]
name = "cek-peer-rust"
description = "CEK Peer native Rust runtime — own JSON wire on kernel, no mint"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

readme = "README.md"
homepage = "https://github.com/bitplorer/cek-runtime"
documentation = "https://docs.rs/cek-peer-rust"
keywords = ["cek", "capability", "authorization"]
categories = ["authentication"]

[dependencies]
cek-contract = { workspace = true }
cek-peer-kernel = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
