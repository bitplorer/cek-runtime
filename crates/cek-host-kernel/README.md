# cek-host-kernel

Reference Host: verify Cap → idempotency → once → dispatch → lineage → project (LAW §11: ability ∪ Baseline) → Result.

Refuse → `ops: []`. Peer does not live in this crate. Missing Manifest → Baseline-only Result Ops.

```toml
cek-host-kernel = "0.1.2"
```
