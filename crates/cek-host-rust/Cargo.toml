[package]
name = "cek-host-rust"
description = "CEK Host native Rust runtime — own JSON wire on kernel, sessionful"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

readme = "README.md"
homepage = "https://github.com/bitplorer/cek-runtime"
documentation = "https://docs.rs/cek-host-rust"
keywords = ["cek", "capability", "authorization"]
categories = ["authentication"]

[dependencies]
cek-contract = { workspace = true }
cek-host-kernel = { workspace = true }
serde_json = { workspace = true }
