# cek-cli

```bash
cek demo
cek vectors [dir]
cek apply      # stdin ApplyRequest JSON → cek-peer-rust (native peer door)
cek host-json  # stdin {cmd:mint|submit} → cek-host-rust (native host door)
```

```toml
# binary: cek
cek-cli = "0.1.3"
```
