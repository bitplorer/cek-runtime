# cek-peer-pyo3

PyO3 hop onto `cek-peer-rust` (native peer JSON door). Apply/receipt only. No mint.

Same JSON documents: `{ result, profile, unknown_op_policy }` →
`{ receipt, kv, ui, log }`. Not a second Peer kernel and not a Cap mint
surface.

```toml
cek-peer-pyo3 = "0.1.2"
```
