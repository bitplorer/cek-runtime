[package]
name = "cek-contract"
description = "CEK interop types, Baseline Ops catalog, and conformance vector loading"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

readme = "README.md"
homepage = "https://github.com/bitplorer/cek-runtime"
documentation = "https://docs.rs/cek-contract"
keywords = ["cek", "capability", "authorization"]
categories = ["authentication"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
