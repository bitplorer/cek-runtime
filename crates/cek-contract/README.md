# cek-contract

CEK wire types, frozen S (Baseline ∪ UI seed), pair identity, structure gate, conformance vectors.

```toml
cek-contract = "0.1.2"
```

Pair = `(ns, name)`. FQ is display only. `("ui.dom","morph")` ≠ `("ui","dom.morph")`.
