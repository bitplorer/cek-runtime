[package]
name = "cek-ops-ui"
description = "CEK domain ui.* world (in-memory snapshot/morph)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

readme = "README.md"
homepage = "https://github.com/bitplorer/cek-runtime"
documentation = "https://docs.rs/cek-ops-ui"
keywords = ["cek", "capability", "authorization"]
categories = ["authentication"]

[dependencies]
serde = { workspace = true }
cek-contract = { workspace = true }
serde_json = { workspace = true }
